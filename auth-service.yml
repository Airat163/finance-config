server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://host.docker.internal:5433/authdb
    username: auth_user
    password: "0206"
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
